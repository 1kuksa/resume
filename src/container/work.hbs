{{!--
Це код container файлу для summary сторінки,
в якому міститься код, котрий буде віддаватися по шляху (PATH) - "/summary"
--}}

<div class="p-2">

    {{#with main}}

    <main>
        <section>
            {{> title data="Work history" big=true}}
            <div>
                <ul>
                    {{#each works}}
                    <li>
                        <div class="pb-2">{{position}}</div>



                        <div>
                            {{duration.from}}
                            -
                            {{#if duration.to }}
                            {{duration.to}}
                            {{else}} (Досі працюємо)
                            {{/if}}
                        </div>


                        <div class="pt-3">{{> link text=company.name href=company.url blank=true noLine=true}}</div>


                        <div class="pt-4 pb-3"><strong>Projects ({{projectAmount}})</strong></div>

                        <ul>
                            {{#each projects}}
                            <li>
                                <div class="pb-3">{{> link text=name href=url blank=true noLine=true}}</div>

                                <div>
                                    <p>
                                        {{about}}
                                    </p>
                                </div>

                                <div class="pt-3">
                                    <div class="mb-3"><em>"Project stacks" ({{stackAmount}})</em></div>

                                    <ul>
                                        {{#each stacks}}
                                        <li>
                                            <div>{{name}}</div>
                                        </li>
                                        {{/each}}
                                    </ul>
                                </div>

                                <div class="pt-3">
                                    <div class="mb-3"><em>" Project awards"</em></div>

                                    <ul>
                                        {{#each awards}}
                                        <li>
                                            <div>{{name}}</div>
                                        </li>
                                        {{/each}}
                                    </ul>
                                </div>
                            </li>
                            {{/each}}
                        </ul>
                    </li>
                    {{/each}}
                </ul>

            </div>
        </section>



    </main>

    {{/with}}

</div>

{{!--
Цей код містить лише те, що буде виводитись в {{{body}}} в layout/default.hbs
Адже наш layout/default.hbs має вже структуру HTML
--}}